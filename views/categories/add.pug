html
  head
    title Add Category

  body
    h1 Add Category

    form(action="/add-category" method="POST")
      label Category Name:
      input(type="text" name="category_name")
      br
      br
      button(type="submit") Save